# Try output templates

Select View → Output Style → Reading Serif. Insert a page break in a synthetic document and export PDF; check page numbers and margins. Export HTML with a local PNG and verify it contains the image without copying the PNG. Custom JSON styles may specify fontFamily, pointSize (8–32), header, footer and titlePage; placeholders are {title}, {page}, {pages}.
