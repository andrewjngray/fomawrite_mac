# Cycle 61 — checkout path exercise

In Fomawrite, open the `fomawrite_mac` favorite, a saved search and a recent sample Markdown file. Confirm each resolves without an unavailable-path message. In Codex, add or relink the project to the physical `fomawrite_mac` folder before continuing development; the old saved project path no longer exists.
