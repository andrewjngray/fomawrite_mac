# Cycle 31 feedback check

- Click Filter files: the label stays inside the field, without floating above the border.
- Type and clear a filter; normal filtering and Escape still work.
- Narrow the pane/window: both rounded ends stay visible until the existing minimum size.
- Check dark appearance on your normal setup.

54 automated tests pass, including narrow geometry. Native focused field inspected. Native resize drag and dark appearance remain user-review items.
