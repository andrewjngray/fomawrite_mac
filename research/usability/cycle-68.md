# Cycle 68 usability — output CSS

Use synthetic content, a small local CSS sample and temporary output.

- [ ] Select valid CSS in HTML mode and confirm it affects HTML export only.
- [ ] Export PDF/share Markdown and confirm neither receives CSS.
- [ ] Try CSS with `url()` or `@import`; confirm rejection retains earlier valid CSS.
- [ ] Clear CSS, restart and confirm displayed selection is accurate.
