# Cycle 71 usability — simpler app icon

Open `dist/Fomawrite Dev.app` and view its Dock icon at your normal Dock size. Check whether the two writing lines and blue caret remain clear. Compare with the previous split-view design in Git history if useful, and report any contrast issue on a dark Dock. The installed `/Applications/Fomawrite.app` is a separate, older bundle.
