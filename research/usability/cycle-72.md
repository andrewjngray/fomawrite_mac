# Cycle 72 usability — final app icon

Open the refreshed `dist/Fomawrite Dev.app` and glance at its Dock icon at your usual size. The short grey connector should make the two writing lines read as a compact F while the blue caret remains visible. If the Dock shows the previous design, macOS may be displaying a cached icon; the generated bundle contains the selected resource. The installed `/Applications/Fomawrite.app` is separate and was not replaced.
