# Cycle 79 review — export and Visual Edit

Open the refreshed `dist/Fomawrite Dev.app` with a sample Markdown document. In Export, check the top alignment, style cards, labels and Save action at both wide and narrow window sizes. In Split view, switch on Visual Edit and compare headings, paragraph text and bullet/numbered lists with the read-only preview. Edit ordinary text, then Undo and save/reopen to confirm source is preserved. Use Source for code, tables and other protected blocks. Report any clipping, cramped control, or type size that still feels wrong.
