# Cycle 16b optional exercise

Use a disposable saved Markdown sample. File → Move To… uses Qt's folder picker on macOS; select a destination folder and click Open. Command-L enters a parent folder path directly, then select the desired child folder.

- [x] Agent verified Cancel leaves the source unchanged.
- [x] Agent verified an existing destination filename produces an error and keeps the original.
- [ ] Add an unsaved sentence, move to an empty destination folder, and verify it remains in the editor.
- [ ] Undo/redo, Save, then reopen from Recents; confirm the file is in the destination and absent from the source folder.
- [ ] Check any relative image/link paths after moving; assets are not relocated automatically.
- [ ] Optional later test: destination on another volume, using disposable content only.

Live success paused because Andrew resumed using Dev. Automated picker success/undo/recovery/watcher and failed-removal checks pass. Andrew's usability feedback is pending. Next feature cycle: 17 search/edit menus.
