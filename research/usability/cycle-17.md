# Cycle 17 optional exercise

- Type `cat CAT cat` into a disposable document.
- Edit → Find → Find and Replace: find `cat`, replace with `dog`, click All.
- Click the editor and Command-Z: all three original words should return in one step.
- Command-G / Shift-Command-G wrap through the matches.
- Select a word and choose Use Selection for Find.
- Select the query text and choose Edit → Delete: the document should remain intact.

Agent verified native replacement, document undo and wrapping; selection/Delete covered by tests. Andrew feedback pending. Next: block formatting. Exact tooltip timing and earlier Move To checks remain open.
