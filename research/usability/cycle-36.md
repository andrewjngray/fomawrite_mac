# Try richer preview blocks

Use /nested/chapter.md, /table.csv or /example.py on a line by itself within a sample folder. Included Markdown should resolve its links/images locally; CSV content is escaped and code remains literal. Add a footnote with four-space continuation paragraphs and follow its return link. Report unsupported nested syntax rather than assuming parity.
