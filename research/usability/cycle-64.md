# Cycle 64 usability — visual editing baseline

Use a disposable sample with paragraph, heading, bold/italic, inline link, table and code block.

- [ ] Toggle Visual Edit without editing; confirm source is unchanged.
- [ ] Replace supported inline text and verify one source Undo restores it.
- [ ] Try table/code/image/multiline edits; confirm refusal and intact Markdown.
- [ ] Try IME input if available, then undo/redo; save/reopen only the sample.
