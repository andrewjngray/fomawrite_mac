# Cycle 73 usability — Dock icon

Launch `/Applications/Fomawrite.app` and `dist/Fomawrite Dev.app`. Confirm that each running Dock tile displays the short-connector writing icon rather than the generic macOS grid placeholder. If an old pinned tile stays generic, compare it with the newly launched running tile; macOS may retain a cached pinned icon.
