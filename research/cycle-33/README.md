# Cycle 33 — Themes

Build and 57 tests pass. Native Aa → Theme choices Warm paper and Dark were exercised on Cycle 32 synthetic documents. Restart retained Dark; both native tabs use the chosen theme. Final correction matches the editor paper to preview and applies native NSWindow appearance to prevent a light tab bar in Dark mode.

Screenshots are synthetic sample content only. Warm-paper screenshot predates the final editor/preview paper match; dark screenshot shows the final surfaces. Inactive macOS tab labels remain faint and belong in the accessibility audit. No native Linux, high-contrast or every-system-picker appearance claim.

Theme changes preserve Markdown/modified state, persist across new backends and are synchronized across existing windows. Print/export templates remain independent. No imported CSS or arbitrary code.
