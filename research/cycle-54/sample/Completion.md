# Completion sample

The lantern glows beside another lantern.
The landscape opens beyond the lake.

Try typing `lan` in a new paragraph and explicitly accepting a local word suggestion.

`lantern` is code and should not receive a completion popup inside this span.

```text
lantern
```

[landscape](https://example.invalid/lantern)
