# Native record — 21a

Automated saved edit/rename/delete and stale-query checks pass. Live exact-tag content search found the sample.

Remaining: Not a persistent index: rereads bounded files. Unsaved buffers are excluded. Tree/list/date options not confirmed beyond existing controls; native large-library stress remains.
