#!/usr/bin/env sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
if [ "$(uname -s)" = Darwin ]; then
  exec "$ROOT/build/Omawrite.app/Contents/MacOS/Omawrite" "$@"
else
  exec "$ROOT/build/omawrite" "$@"
fi
