#!/usr/bin/env sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
if [ "$(uname -s)" = Darwin ]; then
  exec "$ROOT/build/Fomawrite.app/Contents/MacOS/Fomawrite" "$@"
else
  exec "$ROOT/build/fomawrite" "$@"
fi
