#!/usr/bin/env sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
. "$ROOT/bin/qt-env"
mkdir -p "$ROOT/build-tests"
cd "$ROOT/build-tests"
"$QMAKE" "$ROOT/tests/tests.pro"
make -j"$JOBS"
QT_QPA_PLATFORM=offscreen ./tst_fomawrite "$@"
